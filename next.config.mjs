/** @type {import('next').NextConfig} */
/**
 * reactStrictMode
 * 리액트앱에 존재하는 잠재적인 문제를 검사하기 위해서
 * 개발모드로 실행했을 때 컴포넌트를 2번 실행하게 된다.
 */
const nextConfig = {
  reactStrictMode: false, 
};

export default nextConfig;
